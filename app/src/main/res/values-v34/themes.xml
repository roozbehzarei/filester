<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Filester" parent="Theme.AppCompat.NoActionBar" />

    <!-- SplashScreen -->
    <style name="Theme.Filester.Starting" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenIconBackgroundColor">@color/seed</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenBackground">@android:color/system_background_light</item>
        <item name="postSplashScreenTheme">@style/Theme.Filester</item>
    </style>

</resources>